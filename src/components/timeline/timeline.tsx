"use client";

import { useState } from "react";
import type { TimelineEvent, TimelineKind } from "@/types/domain";
import { DetailDrawer } from "@/components/common/detail-drawer";

const filters=["All","Diplomacy","Conflict","Economic","Statements","Agreements"] as const;
export function Timeline({events,filterable=true}:{events:TimelineEvent[];filterable?:boolean}){const [filter,setFilter]=useState<(typeof filters)[number]>("All");const [selected,setSelected]=useState<TimelineEvent|null>(null);const shown=filter==="All"?events:events.filter(e=>e.kind===filter as TimelineKind);return <div>{filterable&&<div className="mb-5 flex flex-wrap gap-2">{filters.map(f=><button key={f} onClick={()=>setFilter(f)} className={`border px-3 py-1.5 text-[9px] ${filter===f?"border-[var(--accent)] bg-white/[.06]":"border-white/10 text-[var(--muted)]"}`}>{f}</button>)}</div>}<div className="relative border-l border-white/12 pl-5">{shown.map(event=><button key={event.id} onClick={()=>setSelected(event)} className="group relative block w-full border-b border-white/[.07] py-5 text-left"><span className="absolute -left-[23px] top-6 h-[5px] w-[5px] rounded-full bg-[var(--accent)] ring-4 ring-[#0b0e11]"/><div className="flex items-center gap-3"><span className="mono text-[9px] text-[var(--accent)]">{event.date}</span><span className="mono text-[8px] uppercase text-[var(--faint)]">{event.kind}</span></div><div className="mt-2 text-sm group-hover:text-[var(--accent)]">{event.title}</div><div className="mt-1 line-clamp-2 text-xs leading-5 text-[var(--muted)]">{event.detail}</div></button>)}</div>{selected&&<DetailDrawer title={selected.title} eyebrow={`${selected.date} · ${selected.kind}`} onClose={()=>setSelected(null)}><p className="text-sm leading-7 text-[#c4c9c7]">{selected.detail}</p><div className="mt-6 border-t border-white/10 pt-4 text-[10px] text-[var(--muted)]">Prototype timeline item · contextual demo data</div></DetailDrawer>}</div>}
