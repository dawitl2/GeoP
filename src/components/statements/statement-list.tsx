"use client";

import { useState } from "react";
import type { Statement } from "@/types/domain";
import { DetailDrawer } from "@/components/common/detail-drawer";
export function StatementList({statements}:{statements:Statement[]}){const [selected,setSelected]=useState<Statement|null>(null);return <div className="grid gap-px bg-white/10 sm:grid-cols-2">{statements.map(s=><button key={s.id} onClick={()=>setSelected(s)} className="bg-[#0b0e11] p-5 text-left hover:bg-[#111519]"><div className="flex items-center justify-between"><span className="eyebrow text-[var(--accent)]">{s.sourceType}</span><span className="mono text-[8px] text-[var(--faint)]">{s.date}</span></div><h3 className="mt-4 text-sm">{s.speaker}</h3><p className="mt-1 text-[10px] text-[var(--muted)]">{s.role} · {s.organization}</p><p className="mt-4 line-clamp-3 text-xs leading-5 text-[#aeb4b2]">{s.summary}</p></button>)}{selected&&<DetailDrawer title={selected.speaker} eyebrow={`${selected.sourceType} · ${selected.date}`} onClose={()=>setSelected(null)}><div className="text-xs text-[var(--muted)]">{selected.role}<br/>{selected.organization}</div><blockquote className="mt-7 border-l border-[var(--blue)] pl-5 text-base leading-7 text-[#d6d9d5]">{selected.summary}</blockquote><div className="mt-7 text-[10px] text-[var(--faint)]">Short fictional summary · direct official-source category</div></DetailDrawer>}</div>}
